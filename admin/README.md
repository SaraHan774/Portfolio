# Portfolio Admin

포트폴리오 작품 관리용 어드민 대시보드.

React + TypeScript, Ant Design, Firebase (Auth, Firestore, Storage) 기반.

```bash
npm install && npm run dev        # 개발
npm run build && firebase deploy  # 배포
```
