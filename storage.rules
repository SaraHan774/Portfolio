rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Emulator에서는 모든 읽기/쓰기 허용
      // 프로덕션 규칙은 Firebase Console에서 별도 관리
      allow read, write: if request.auth != null;
    }
  }
}
